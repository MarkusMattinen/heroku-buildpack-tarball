#!/usr/bin/env bash

set -e            # fail fast
set -o pipefail   # dont ignore exit codes when piping output
# set -x          # enable debugging

cd $1

tar zxf app.tar.gz
rm app.tar.gz

mv app/* app/.* .
rmdir app
